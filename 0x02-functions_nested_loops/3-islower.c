#include "main.h"

/**
 * main - checks code
 *
 * Returns zero when successful
 *
 * _islower checks if param is small case
 */

int _islower (int c)
{
if (c <= 'a' && c >= 'z')
{
return (1);
}
else
{
return (0);
}
}
