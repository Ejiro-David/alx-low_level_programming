#include "main.h"

/**
 * _abs - checks for the absolute value of
 * @n: the integer.
 * Return: Absolute value of the int.
 */

int _abs(int n)
{
if (n >= 0)
{
	return (n);
}
else
{
	return (-n);
}
}
