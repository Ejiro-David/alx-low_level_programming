#include "main.h"

/**
 * mul - multiplies two ints
 * @a: one int to be timesd by another
 * @b: second into to be timesd
 * Return: the result of the timsing
 */

int mul(int a, int b)
{
	return (a * b);
}
