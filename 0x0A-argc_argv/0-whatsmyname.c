#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the name of itself
 * @argc: argument count
 * @argv: argument vector
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
